import { defineConfig } from "vite";

export default defineConfig({
    //base: '/ThreeJS-Projects/', // the gh pages repo name, you don't need to set this if just copying the build files over.
    // See https://www.youtube.com/watch?v=yo2bMGnIKE8
})